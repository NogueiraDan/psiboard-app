import { SafeAreaView, Text, View } from "react-native";

export default function Schedules() {
  return (
    <SafeAreaView className="bg-slate-200 flex-1 justify-center items-center">
      <Text className="text-3xl">Schedules Screen</Text>
    </SafeAreaView>
  );
}
